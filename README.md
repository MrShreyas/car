This Project is currently working on windows for other platforms be cautious before running.

Car project is configured with CMake so no need to run extra command unless you add another dependency

To run this project run following commands:


    mkdir build
    
    cd build
    
    cmake ..
    
    cmake --build .
